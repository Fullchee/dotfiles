---
description: Better tools
---

- Use `rg` for searching. (Do not use `grep`)
- Use `fd` for finding files (Do not use `find`)
- Use `sd` for replacing text (Do not use `sed`)
- Use `difft` (difftastic) for comparing files (Do not use `diff`)
- jc and jq can be used for parsing JSON and other structured data formats
