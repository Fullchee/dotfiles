[alias]
	# without the `!` prefix: limited to executing Git commands
		# eg: `test = git log`: `git test` => `git git log`
	# with `!`: can run arbitrary commands
		# means that you can run arbitrary code, like &&
		# eg: myscript = !git fetch origin && git merge origin/main
		# restore-branch = "!f() { git fetch; git restore --source origin/$1 $2; }; f"

	# typos
	pul = pull
	pus = push

	#### log
	adog = log --all --decorate --oneline --graph
	amend = !git add ${1-.} && git commit --amend --no-edit
	cp-last-hash="!git rev-parse HEAD | pbcopy"
	dft = difftool
	dlog = "!f() { : git log ; GIT_EXTERNAL_DIFF=difft git log -p --ext-diff $@; }; f"

	make-executable = update-index --chmod=+x
	view-executables = ls-files --stage

	ignore = update-index --skip-worktree  # only for files
	# for folders: add to `.git/info/exclude`
	unignore = update-index --no-skip-worktree
	ignored = !git ls-files -v | grep "^[hS]"

	# Append path to info/exclude
    # Uses $(git rev-parse --git-common-dir) to find the shared .git folder (works in worktrees too)
    exclude-untracked = "!f() { echo \"$1\" >> \"$(git rev-parse --git-common-dir)/info/exclude\"; }; f"
	unexclude-untracked = "!f() { sed -i \"\" \"\\|^$1$|d\" \"$(git rev-parse --git-common-dir)/info/exclude\"; }; f"
	view-excluded = !cat \"$(git rev-parse --git-common-dir)/info/exclude\"

	empty-commit = !git commit --allow-empty -m 'Empty commit: trigger GH actions'

	# make the file exactly the same as branch $1
	# git restore-branch branch_name file_name
	restore-branch = "!f() { git fetch; git restore --source origin/$1 $2; }; f"
	restore-deleted = !git ls-files --deleted -z | xargs -0 git rm
	restore-file = "!f() { git restore --source origin/main -- $@; }; f"
	revert-file = restore-file
	restore-files = restore-file
	restore-files-from-branch = "!f() { git restore --source origin/$1 -- ${@:2}; }; f"
	restore-folder = restore --source=origin/main --staged --worktree --
	revert-folder = restore-folder
	# Makes the file exactly the same as main in origin/main
    # git restore-main file1 file2
    restore-main = restore --source origin/main

	search-branches = "!f() { git grep $1 `git show-ref --heads`}"
	set-upstream = !git branch --set-upstream-to=origin/$(git symbolic-ref --short HEAD)
	st = status -s
	squash-commits = !git reset --soft HEAD~3 && git commit --edit -m"$(git log --format=%B --reverse HEAD..HEAD@{1})"
	updated-files= !git log --since="7 days ago" --name-only --pretty=format: | sort | uniq | /opt/homebrew/bin/tree --fromfile

[blame]
	ignoreRevsFile = .git-blame-ignore-revs

[branch]
	sort = -committerdate
[color]
	ui = auto
[column]
	ui = auto
[core]
	editor = "hx"
	excludesfile = ~/.gitignore_global
	pager = delta

	# launch a process for each active repo
	# fsmonitor = true
	# untrackedCache = true
[commit]
	template = ~/.gitmessage
	# add commit diff in text editor where I'm writing the commit message
	verbose = true
	gpgsign = true
[credential]
	helper = store
[credential "https://github.com"]
	helper =
	helper = !/usr/local/bin/gh auth git-credential
[credential "https://gist.github.com"]
	helper =
	helper = !/usr/local/bin/gh auth git-credential
[delta]
    navigate = true  # use n and N to move between diff sections
[diff]
	algorithm = histogram
	external = difft
	tool = difftastic
	colorMoved = plain
	mnemonicPrefix = true
	renames = true
[difftool]
    prompt = false
[difftool "vscode"]
    cmd = "code --wait --diff  "
[fetch]
	prune = true
	pruneTags = true
	all = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[gpg]
	format = ssh
[help]
	autocorrect = 10
[init]
	defaultBranch = main
[interactive]
    diffFilter = delta --color-only
[merge]
	# manual fallback tool
	tool = weave
	# use zdiff3 when conflicts do happen
	conflictstyle = zdiff3
[merge "weave"]
    # Use Weave as the automatic background driver
    name = Weave semantic merge driver
    driver = weave merge %O %A %B %L %P
    recursive = binary
[mergetool "vscode"]
	cmd = code --wait $MERGED
[open "https://ssh.github.com"]
	domain = github.com
[push]
	# always push the local branch to a remote branch with the same name
	autoSetupRemote = true
	default = simple
	followTags = true
[pull]
	# convenient: if rebase works, great!
	# if it doesn't work: just `git rebase --abort` and `git merge origin/main`
	rebase = true
[rebase]
	# `git commit --fixup OLD_COMMIT_ID` sets the commit message to 'fixup! old_commit message'
	# `git rebase --autosquash main` will combine all the fixup! commits with their targets
	# --autosquash gets auto passed to git rebase
	autosquash = true
	autoStash = true
	updateRefs = true
[rerere]
	# remember how you resolved merge conflicts during `git rebase` and auto resolves conflicts when it can
	enabled = true
	autoupdate = true
[submodule]
	recurse = true
[tag]
	sort = version:refname
[url "git@github.com:"]
	insteadOf = "https://github.com/"
[user]
{{- if eq .chezmoi.hostname "Fullchee-Citylitics" }}
	email = 243146669+fullchee-citylitics@users.noreply.github.com
{{- else }}
	email = 11246258+Fullchee@users.noreply.github.com
{{- end }}
	name = Fullchee Zhang
	signingkey = ~/.ssh/id_ed25519.pub

{{- if eq .chezmoi.hostname "Fullchee-Citylitics" }}
# must be at bottom to override above
[includeIf "gitdir:~/personal-projects/**"]
	path = ~/.dotfiles/work/personal-projects.gitconfig
[includeIf "gitdir:/Users/admin/.local/share/chezmoi/**"]
	path = ~/.dotfiles/work/personal-projects.gitconfig
{{- end }}
